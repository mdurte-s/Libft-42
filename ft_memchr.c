#include <stdio.h>

void	*ft_memchr(const void *s, int c, size_t n);

int	main(int argc, char **argv)
{
	int	i;

	i = 0;
	while (++i < argc)
		printf("'%c' %d\n", argv[i], ft_(argv[i]));
	return (0);
}

int	ft_()
{
	
}